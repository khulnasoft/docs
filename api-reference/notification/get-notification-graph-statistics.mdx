---
openapi: get /v1/notifications/graph/stats
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>

```bash cURL
curl --request GET \
--url https://api.teleflow.khulnasoft.com/v1/notifications/graph/stats \
--header 'Authorization: ApiKey <TELEFLOW_API_KEY>'
```

```python Python
from teleflow.api import NotificationApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"

notificationApi = NotificationApi(url, api_key)

graphStats = notificationApi.graph_stats()
list(graphStats)
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow('<TELEFLOW_API_KEY>');

$notificationGraphStats = $teleflow->getNotificationGraphStats()->toArray();
```

</RequestExample>