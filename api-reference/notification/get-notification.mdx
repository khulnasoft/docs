---
openapi: get /v1/notifications/{notificationId}
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>

```kotlin Kotlin
import co.teleflow.Teleflow
import co.teleflow.extensions.notification

fun main() {
    val teleflow = Teleflow(apiKey = "<TELEFLOW_API_KEY>")
    val notification = teleflow.notification("<NOTIFICATION_ID>")
    println(notification)
}
```

```bash cURL
curl --request GET \
--url https://api.teleflow.khulnasoft.com/v1/notifications/{notificationId} \
--header 'Authorization: ApiKey <TELEFLOW_API_KEY>'
```

```python Python
from teleflow.api import NotificationApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"

notificationApi = NotificationApi(url, api_key)

notification = notificationApi.get("<notificationId>")
print(notification)
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow('<TELEFLOW_API_KEY>');

$notification = $teleflow->getNotification($notificationId)-toArray();
```

</RequestExample>
