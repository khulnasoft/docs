---
openapi: get /v1/notifications/stats
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>

```kotlin Kotlin
import co.teleflow.Teleflow
import co.teleflow.extensions.notificationsStats

fun main() {
    val teleflow = Teleflow(apiKey = "<TELEFLOW_API_KEY>")
    val notificationsStats = teleflow.notificationsStats()
    println(notificationsStats)
}
```

```bash cURL
curl --request GET \
--url https://api.teleflow.khulnasoft.com/v1/notifications/stats \
--header 'Authorization: ApiKey <TELEFLOW_API_KEY>'
```

```python Python
from teleflow.api import NotificationApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"

notificationApi = NotificationApi(url, api_key)

notification_stats = notificationApi.stats()
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow('<TELEFLOW_API_KEY>');

$notificationStats = $teleflow->getNotificationStats()->toArray();
```

</RequestExample>