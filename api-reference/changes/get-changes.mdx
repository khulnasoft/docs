---
openapi: get /v1/changes
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>
```javascript Node.js
import { Teleflow } from '@teleflow/node';

const teleflow = new Teleflow('<TELEFLOW_API_KEY>');
await teleflow.changes.get();
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow(<TELEFLOW_API_KEY>);
$teleflow->getChanges();
```

```ruby Ruby
require 'teleflow'

client = Teleflow::Client.new('<TELEFLOW_API_KEY>')

client.changes({
    'page' => 1, # optional
    'limit' => 10, # optional
    'promoted' => 'hello'
})
```

```python Python
from teleflow.api import ChangeApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"
teleflow = ChangeApi(url, api_key).list(
    page = "<Page to retrieve>",
    limit = "<Size of page>",
)
```

```java Java
import co.teleflow.sdk.Teleflow;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<TELEFLOW_API_KEY>";

        Teleflow teleflow = new Teleflow(apiKey);
        teleflow.changes(query = {});
    }
}
```

</RequestExample>

<ResponseExample>

```json Response
{
    totalCount: 0,
    data: [data],
    pageSize: 0,
    page: 0
}
```

</ResponseExample>