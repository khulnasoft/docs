---
openapi: post /v1/changes/{changeId}/apply
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>
```javascript Node.js
import { Teleflow } from '@teleflow/node';

const teleflow = new Teleflow('<TELEFLOW_API_KEY>');
await teleflow.changes.applyOne("changeId");
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow(<TELEFLOW_API_KEY>);
$teleflow->applyChange($changeId, []);
```

```ruby Ruby
require 'teleflow'

client = Teleflow::Client.new('<TELEFLOW_API_KEY>')

client.apply_change(`<change_id≥`)
```

```python Python
from teleflow.api import ChangeApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"
teleflow = ChangeApi(url, api_key).apply(
    change_id = "<A change ID to apply>",
)
```

```java Java
import co.teleflow.sdk.Teleflow;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<TELEFLOW_API_KEY>";

        Teleflow teleflow = new Teleflow(apiKey);
        teleflow.applyChange('changeId');
    }
}
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": [
    {
      "_id": "string",
      "_creatorId": "string",
      "_environmentId": "string",
      "_organizationId": "string",
      "_entityId": "string",
      "enabled": true,
      "type": "Feed",
      "change": {},
      "createdAt": "string",
      "_parentId": "string"
    }
  ]
}
```

</ResponseExample>