---
openapi: delete /v1/workflows/{workflowId}
---

---
openapi: post /v1/workflows
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>

```bash cURL
  curl --request DELETE \
  --url https://api.teleflow.khulnasoft.com/v1/workflows/{workflowId} \
  --header 'Authorization: <authorization>' \
```

```javascript Node.js
import { Teleflow, TemplateVariableTypeEnum, FilterPartTypeEnum, StepTypeEnum } from '@teleflow/node';

const teleflow = new Teleflow('<TELEFLOW_API_KEY>');

await teleflow.notificationTemplates.delete("workflowId");
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow(<TELEFLOW_API_KEY>);
$teleflow->deleteNotificationTemplate($templateId);
```

```ruby Ruby
require 'teleflow'

client = Teleflow::Client.new('<TELEFLOW_API_KEY>')

client.delete_notification_template('<insert-template-id>')
```

```python Python
from teleflow.api import NotificationTemplateApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"
teleflow = NotificationTemplateApi(url, api_key).delete(
    notification_template_id="<The notification workflow ID of the workflow to be deleted >"
)
```

```java Java
import co.teleflow.Teleflow;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<TELEFLOW_API_KEY>";

        Teleflow teleflow = new Teleflow(apiKey);
        teleflow.deleteWorkflow(workflowId);
    }
}
```

```go Go
package main

import (
  "fmt"
  "net/http"
  "encoding/json"
)

func main() {
  url := "https://api.teleflow.khulnasoft.com/v1/workflows/:workflowId"
  data := map[string]string{
    [options]
  }
  jsonValue, _ := json.Marshal(data)
  req, _ := http.NewRequest("DELETE", url, bytes.NewBuffer(jsonValue))
  req.Header.Set("Content-Type", "application/json")
  req.Header.Set("Authorization", "ApiKey REPLACE_WITH_API_KEY")

  defer resp.Body.Close()
  body, _ := ioutil.ReadAll(resp.Body)
  fmt.Println(string(body))
}
```

```kotlin Kotlin
import co.teleflow.Teleflow
import co.teleflow.extensions.deleteWorkflow

class Main {
    fun main(args: Array<String>) {
        val apiKey = "<TELEFLOW_API_KEY>"

        val teleflow = Teleflow(apiKey)
        teleflow.deleteWorkflow(workflowId)
    }
}
```

</RequestExample>

<ResponseExample>

```json Response
{
    "data": true
}
```

</ResponseExample>