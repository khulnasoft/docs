---
openapi: get /v1/execution-details
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>
```bash cURL
curl --request GET \
    --url https://api.teleflow.khulnasoft.com/v1/execution-details \
    --header 'Authorization: <authorization>' 
 ```

```javascript Node.js
import fetch from 'node-fetch';

const response = await fetch('https://api.teleflow.khulnasoft.com/v1/execution-details', {
  method: 'GET',
  
});
const data = await response.json();
```


```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow(<TELEFLOW_API_KEY>);

// Get execution details
$teleflow->getExecutionDetails([
    'notificationId' => '<insert-notification-id>',
    'subscriberId'   => '<insert-subscriber-id>'
])->toArray();
```

```ruby Ruby
require 'teleflow'

client = Teleflow::Client.new('<TELEFLOW_API_KEY>')

client.execution_details({
    'notificationId' => '8fxxx-xxx-ef9xxxxce66',
    'subscriberId' => '7789'
})
```

```python Python
from teleflow.api import ExecutionDetailApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"
teleflow = ExecutionDetailApi(url, api_key).list(
    notification_id = "<The notification ID in Teleflow>",
    subscriber_id = "<The subscriber ID in Teleflow>",
)
```

```java Java
import co.teleflow.sdk.Teleflow;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<TELEFLOW_API_KEY>";

        Teleflow teleflow = new Teleflow(apiKey);
        teleflow.executionDetails(query = {});
    }
}
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": [
    {
      "_id": "string",
      "_organizationId": "string",
      "_jobId": "string",
      "_environmentId": "string",
      "_notificationId": "string",
      "_notificationTemplateId": "string",
      "_subscriberId": "string",
      "_messageId": "string",
      "providerId": "string",
      "transactionId": "string",
      "channel": "in_app",
      "detail": "string",
      "source": "Credentials",
      "status": "Success",
      "isTest": true,
      "isRetry": true,
      "createdAt": "string"
    }
  ]
}
```

</ResponseExample>