---
openapi: get /v1/integrations
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>
```javascript Node.js
import { Teleflow } from '@teleflow/node';  

const teleflow = new Teleflow('<TELEFLOW_API_KEY>');

await teleflow.integrations.getAll();
```

```php PHP
// Get integrations
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow(<TELEFLOW_API_KEY>);
$teleflow->getIntegrations()->toArray();
```
```ruby Ruby
require 'teleflow'

client = Teleflow::Client.new('<TELEFLOW_API_KEY>')
client.integrations()
```
```go Go
package main

import (
	"context"
	"fmt"
	teleflow "github.com/khulnasoft/go-teleflow/lib"
	"log"
)

func main() {
    // get integrations
	integrations, err := teleflowClient.IntegrationsApi.GetAll(ctx)
	if err != nil {
		log.Fatal("Get all integrations error: ", err.Error())
	}
	fmt.Println(integrations)
}
```
```python Python
from teleflow.api import IntegrationApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"
teleflow = IntegrationApi(url, api_key).list(
)
```

```java Java
import co.teleflow.sdk.Teleflow;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<TELEFLOW_API_KEY>";

        Teleflow teleflow = new Teleflow(apiKey);
        teleflow.integrations()
    }
}
```


</RequestExample>

<ResponseExample>

```json Response
{
    _id: "_id",
    _environmentId: "_environmentId",
    _organizationId: "_organizationId",
    name: "name",
    identifier: "identifier",
    providerId: "providerId",
    channel: "channel",
    credentials: credentials,
    active: true,
    deleted: true,
    deletedAt: "deletedAt",
    deletedBy: "deletedBy",
    primary: true
}
```

</ResponseExample>