---
openapi: delete /v1/feeds/{feedId}
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>

```bash cURL
curl --request DELETE \
--url https://api.teleflow.khulnasoft.com/v1/feeds/{FEED_IDENTIFIER} \
--header 'Authorization: ApiKey <TELEFLOW_API_KEY>'
```

```javascript Node.js
import { Teleflow } from '@teleflow/node';

const teleflow = new Teleflow('<TELEFLOW_API_KEY>');
await teleflow.feeds.delete("<Feed_Id>");
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow(<TELEFLOW_API_KEY>);

$teleflow->deleteFeed();
```

```ruby Ruby
require 'teleflow'

client = Teleflow::Client.new('<TELEFLOW_API_KEY>')

client.delete_feed("<Feed_Id>")
```

```python Python
from teleflow.api import FeedApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"
teleflow = FeedApi(url, api_key).delete(
    Feed_Id = "<ID of the feed to be deleted>",
)
```

```java Java
import co.teleflow.sdk.Teleflow;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<TELEFLOW_API_KEY>";

        Teleflow teleflow = new Teleflow(apiKey);
        teleflow.deleteFeed(Feed_Id);
    }
}
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": [
    {
      "_id": "string",
      "name": "string",
      "identifier": "string",
      "_environmentId": "string",
      "_organizationId": "string"
    }
  ]
}
```

</ResponseExample>