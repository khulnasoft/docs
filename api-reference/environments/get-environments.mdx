---
openapi: get /v1/environments
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>
```javascript Node.js
import { Teleflow } from '@teleflow/node';

const teleflow = new Teleflow('<TELEFLOW_API_KEY>');

await teleflow.environments.getAll();
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow(<TELEFLOW_API_KEY>);
$teleflow->getEnvironments()->toArray();
```

```ruby Ruby
require 'teleflow'

client = Teleflow::Client.new('<TELEFLOW_API_KEY>')
client.environments()
```

```python Python
from teleflow.api import EnvironmentApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"
teleflow = EnvironmentApi(url, api_key).list(
)
```

```java Java
import co.teleflow.sdk.Teleflow;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<TELEFLOW_API_KEY>";

        Teleflow teleflow = new Teleflow(apiKey);
        teleflow.environments();
    }
}
```

```kotlin Kotlin
import co.teleflow.Teleflow
import co.teleflow.extensions.environments

fun main() {
    val teleflow = Teleflow(apiKey = "API_KEY")
    val environment = teleflow.environments()
    println(environment)
}
```

```bash cURL
curl --request GET \
     --url https://api.teleflow.khulnasoft.com/v1/environments \
     --header 'Authorization: ApiKey <TELEFLOW_API_KEY>'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": [
    {
      "_id": "string",
      "name": "string",
      "_organizationId": "string",
      "identifier": "string",
      "apiKeys": [
        {
          "key": "string",
          "_userId": "string"
        }
      ],
      "_parentId": "string"
    }
  ]
}
```

</ResponseExample>