---
openapi: post /v1/environments/api-keys/regenerate
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>
```javascript Node.js
import { Teleflow } from '@teleflow/node';

const teleflow = new Teleflow('<TELEFLOW_API_KEY>');
await teleflow.environmets.regenerateApiKeys();
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow(<TELEFLOW_API_KEY>);
$key = $teleflow->regenerateEnvironmentsAPIKeys()->toArray();
```

```ruby Ruby
require 'teleflow'

client = Teleflow::Client.new('<TELEFLOW_API_KEY>')
client.regenerate_api_keys()
```

```java Java
import co.teleflow.sdk.Teleflow;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<TELEFLOW_API_KEY>";

        Teleflow teleflow = new Teleflow(apiKey);
        teleflow.regenerateApiKeys();
    }
}
```

```python Python
from teleflow.api import EnvironmentApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"
regenerateApiKey = EnvironmentApi(url, api_key).regenerate_api_key()
```

```bash cURL
curl --request POST \
     --url https://api.teleflow.khulnasoft.com/v1/environments/api-keys/regenerate \
     --header 'Authorization: ApiKey <TELEFLOW_API_KEY>'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": [
    {
      "key": "string",
      "_userId": "string"
    }
  ]
}
```

</ResponseExample>