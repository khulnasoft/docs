---
openapi: get /v1/environments/api-keys
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>
```javascript Node.js
import { Teleflow } from '@teleflow/node';

const teleflow = new Teleflow('<TELEFLOW_API_KEY>');
await teleflow.environmets.getApiKeys();
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow(<TELEFLOW_API_KEY>);
$teleflow->getEnvironmentsAPIKeys()->toArray();
```

```ruby Ruby
require 'teleflow'

client = Teleflow::Client.new('<TELEFLOW_API_KEY>')
client.api_keys()
```

```java Java
import co.teleflow.sdk.Teleflow;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<TELEFLOW_API_KEY>";

        Teleflow teleflow = new Teleflow(apiKey);
        teleflow.apiKeys();
    }
}
```

```python Python
from teleflow.api import EnvironmentApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"
apiKeys = EnvironmentApi(url, api_key).api_keys()
```

```kotlin Kotlin
import co.teleflow.Teleflow
import co.teleflow.extensions.apiKeys

fun main() {
    val teleflow = Teleflow(apiKey = "API_KEY")
    val apiKeys = teleflow.apiKeys()
    println(apiKeys)
}
```

```bash cURL
curl --request GET \
     --url https://api.teleflow.khulnasoft.com/v1/environments/api-keys \
     --header 'Authorization: ApiKey <TELEFLOW_API_KEY>'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": [
    {
      "key": "string",
      "_userId": "string"
    }
  ]
}
```

</ResponseExample>