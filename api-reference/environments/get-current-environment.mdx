---
openapi: get /v1/environments/me
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>
```javascript Node.js
import { Teleflow } from '@teleflow/node';

const teleflow = new Teleflow('<TELEFLOW_API_KEY>');

await teleflow.environments.getCurrent();
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow(<TELEFLOW_API_KEY>);

$teleflow->getCurrentEnvironment()->toArray();
```

```ruby Ruby
require 'teleflow'

client = Teleflow::Client.new('<TELEFLOW_API_KEY>')

client.current_environment()
```

```python Python
from teleflow.api import EnvironmentApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"
teleflow = EnvironmentApi(url, api_key).current(
)
```

```java Java
import co.teleflow.sdk.Teleflow;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<TELEFLOW_API_KEY>";

        Teleflow teleflow = new Teleflow(apiKey);
        teleflow.currentEnvironment();
    }
}
```

```kotlin Kotlin
import co.teleflow.Teleflow
import co.teleflow.extensions.currentEnvironment

fun main() {
    val teleflow = Teleflow(apiKey = "API_KEY")
    val currentEnvironment = teleflow.currentEnvironment()
    println(currentEnvironment)
}
```

```bash cURL
curl --request GET \
     --url https://api.teleflow.khulnasoft.com/v1/environments/me \
     --header 'Authorization: ApiKey <TELEFLOW_API_KEY>'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": {
    "_id": "string",
    "name": "string",
    "_organizationId": "string",
    "identifier": "string",
    "apiKeys": [
      {
        "key": "string",
        "_userId": "string"
      }
    ],
    "_parentId": "string"
  }
}
```

</ResponseExample>