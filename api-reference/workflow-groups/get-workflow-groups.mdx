---
openapi: get /v1/notification-groups
---

<Snippet file="apikey-warning.mdx" />

<RequestExample>
```bash cURL
curl --request GET \
--url https://api.teleflow.khulnasoft.com/v1/notification-groups \
--header 'Authorization: ApiKey <TELEFLOW_API_KEY>' \
```

```javascript Node.js
import { Teleflow, TemplateVariableTypeEnum, FilterPartTypeEnum, StepTypeEnum } from '@teleflow/node';

const teleflow = new Teleflow('<TELEFLOW_API_KEY>');

await teleflow.notificationGroups.get();
```

```php PHP
use Teleflow\SDK\Teleflow;

$teleflow = new Teleflow(<TELEFLOW_API_KEY>);

$teleflow->getNotificationGroups()->toArray();
```

```ruby Ruby
require 'teleflow'

client = Teleflow::Client.new('<TELEFLOW_API_KEY>')

client.notification_groups()
```

```python Python
from teleflow.api import NotificationGroupApi

url = "https://api.teleflow.khulnasoft.com"
api_key = "<TELEFLOW_API_KEY>"
teleflow = NotificationGroupApi(url, api_key).list(
)
```

```java Java
import co.teleflow.sdk.Teleflow;

public class Main {
    public static void main(String[] args) {
        String apiKey = "<TELEFLOW_API_KEY>";

        Teleflow teleflow = new Teleflow(apiKey);
        teleflow.notificationGroups();
    }
}
```

```go Go
package main

import (
  "fmt"
  "net/http"
  "encoding/json"
)

func main() {
  url := "https://api.teleflow.khulnasoft.com/v1/notification-groups"
  data := map[string]string{
    [options]
  }
  jsonValue, _ := json.Marshal(data)
  req, _ := http.NewRequest("GET", url, bytes.NewBuffer(jsonValue))
  req.Header.Set("Content-Type", "application/json")
  req.Header.Set("Authorization", "ApiKey REPLACE_WITH_API_KEY")

  defer resp.Body.Close()
  body, _ := ioutil.ReadAll(resp.Body)
  fmt.Println(string(body))
}
    
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": [
    {
      "_id": "string",
      "name": "string",
      "_environmentId": "string",
      "_organizationId": "string",
      "_parentId": "string"
    }
  ]
}
```

</ResponseExample>