---
custom_edit_url: "https://github.com/khulnasoft/khulnasoft/edit/master/src/web/api/formatters/value/README.md"
sidebar_label: "Value formatter"
docs_status: "Published"
docs_rel_path: "Developer and Contributor Corner/REST API/Formatters"
sidebar_position: "1603040"
docs_link: "https://docs.khulnasoft.com/docs/developer-and-contributor-corner/rest-api/formatters/value-formatter"
---

# Value formatter

The Value formatter presents [results of database queries](/docs/developer-and-contributor-corner/rest-api/queries) as a single value.

To calculate the single value to be returned, it sums the values of all dimensions.

The Value formatter respects the following API `&options=`:

| option    | supported | description |
|:----:     |:-------:  |:----------  |
| `percent` | yes       | to replace all values with their percentage over the row total|
| `abs`     | yes       | to turn all values positive, before using them |
| `min2max` | yes       | to return the delta from the minimum value to the maximum value (across dimensions)|

The Value formatter is not exposed by the API by itself.
Instead it is used by the [`ssv`](/docs/developer-and-contributor-corner/rest-api/formatters/ssv-formatter) formatter
and [health monitoring queries](/docs/alerts-&-notifications).


