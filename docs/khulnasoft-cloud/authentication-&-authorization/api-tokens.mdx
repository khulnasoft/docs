---
custom_edit_url: "https://github.com/khulnasoft/khulnasoft/edit/master/docs/khulnasoft-cloud/authentication-and-authorization/api-tokens.md"
sidebar_label: "API Tokens"
docs_status: "Published"
docs_rel_path: "Khulnasoft Cloud/Authentication & Authorization"
sidebar_position: "507010"
docs_link: "https://docs.khulnasoft.com/docs/khulnasoft-cloud/authentication-&-authorization/api-tokens"
---

# API Tokens

API tokens (Bearer tokens) enable programmatic access to Khulnasoft resources. These tokens authenticate and authorize API requests, allowing you to interact with Khulnasoft services securely from external applications, scripts, or integrations.

> **Important**: API tokens never expire but should be managed carefully as they grant access to your Khulnasoft resources.

## Token Generation

**Location**:

Access token management through the Khulnasoft UI:

1. Click your profile picture in the bottom-left corner
2. Select "User Settings"
3. Navigate to the API Tokens section

**Available Scopes**:

Each token can be limited to specific scopes that define its access permissions:

| Scope                  | Description                                                                                                                                        |
|:-----------------------|:---------------------------------------------------------------------------------------------------------------------------------------------------|
| `scope:all`            | Grants the same permissions as the user who created the token. Use case: Terraform provider integration.                                           |
| `scope:agent-ui`       | Used by Agent for accessing the Cloud UI                                                                                                           |
| `scope:grafana-plugin` | Used for the [Khulnasoft Grafana plugin](/docs/dashboards-and-charts/grafana-data-source-plugin) to access Khulnasoft charts |

> **Info**
>
> Currently, Khulnasoft Cloud is not exposing the stable API.

## Example usage

**get the Khulnasoft Cloud space list**

```console
curl -H 'Accept: application/json' -H "Authorization: Bearer <token>" https://app.khulnasoft.com/api/v2/spaces
```
